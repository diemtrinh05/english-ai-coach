# Shared components

Component dùng chung được tổ chức theo các nhóm canonical `layout`, `table`,
`form`, `feedback`, `dialog` và `charts`. `ADM-FND-001` chỉ triển khai
`layout/AdminBootstrapShell`; các nhóm còn lại được thêm khi feature có nhu cầu
thực tế để tránh placeholder API.
